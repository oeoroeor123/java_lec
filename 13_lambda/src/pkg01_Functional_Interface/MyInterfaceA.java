package pkg01_Functional_Interface;

/*
 * @FunctionalInterface Annotation
 * 1. 함수형 인터페이스
 * 2. 함수형 프로그램을 위해 도입되었다.
 * 3. 오직 한 개의 추상 메소드만 가질 수 있다.
 * (인터페이스는 추상 클래스로 본문이 없기때문에, 인스턴스 생성 불가능 -> 본문을 만들어주면 인스턴스 생성 가능해짐)
 */

@FunctionalInterface
public interface MyInterfaceA {
void mathod();


  
  
  
}
